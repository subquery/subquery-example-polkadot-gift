{
  "name": "polkadot-gift-subql",
  "version": "0.0.1",
  "description": "subql project for polkadot gift",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "michael.zhai@onfanility.io",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^5.7.1",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest"
  }
}